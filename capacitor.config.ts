import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.neovation.iplanner',
  appName: 'iPlanner',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
